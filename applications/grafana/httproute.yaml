apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: monitoring
spec:
  parentRefs:
    - name: public
      namespace: envoy-gateway-system
      sectionName: https
  hostnames:
    - grafana.k8s.749rmw.com
  rules:
    - backendRefs:
        - name: kube-prometheus-stack-grafana
          port: 80
