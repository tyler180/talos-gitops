apiVersion: apps/v1
kind: Deployment
metadata:
  name: mm-inches
  namespace: unit-conversions
  labels:
    app.kubernetes.io/name: mm-inches
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: mm-inches
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mm-inches
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: mm-inches
          image: tyler180/mm-inches@sha256:3297150aa24e03831c7f0bbf6e0d638d04d9aa53717912072187eab2de32f09f
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 100m
              memory: 64Mi
          securityContext:
            runAsUser: 65532
            runAsGroup: 65532
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: mm-inches
