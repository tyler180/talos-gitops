apiVersion: apps/v1
kind: Deployment
metadata:
  name: mm-inches
  namespace: unit-conversions
  labels:
    app.kubernetes.io/name: mm-inches
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: mm-inches
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mm-inches
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: mm-inches
          image: tyler180/mm-inches@sha256:ebcb68faadabec3ddeca8970301f43df6b12271f8d9851421f4f1dcec08d1ffd
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 100m
              memory: 64Mi
          securityContext:
            runAsUser: 65532
            runAsGroup: 65532
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: mm-inches
